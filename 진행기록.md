# RDWsGallery
260529
Spring Boot JPA Query DSL 기술을 활용한 게시판 프로젝트 구상 (게시판 기능, 회원가입/로그인 기능, 관리자 페이지 기능) 

260601
Oracle DB에 USER_ACCOUNT, BOARD 테이블 정의
Spring Boot로 DB 테이블과 1:1 매핑하기 위한 UserAccountEntity, BoardEntity, BoardImageEntity 엔티티 초안 작성
코드 재사용을 위해 생성 시간과 수정 시간 컬럼을 BaseEntity 엔티티로 분리
유저 권한과 계정 상태 컬럼의 메타데이터를 enum과 AttributeConverter로 설정

260602
Thymeleaf 문법을 사용한 login.html, signup.html 파일 초안과 user.css 파일 작성
Notyf와 Axios 라이브러리를 추가하여 Ajax 비동기 통신 기술이 적용된 login.js, signup.js 파일 작성

260604
View 역할의 PageRenderController 파일 작성
로그인/회원가입 기능을 위한 userAccountRepository 인터페이스 파일과 UserAccountAPIController 파일 작성
application.yml에 Query DSL 설정 적용하고 QueryDSLConfig 파일 작성
회원가입과 관련된 UserRegisterRequestDTO, 로그인과 관련된 UserCheckLoginRequestDTO 파일 작성

260605
UserAccountAPIController의 비즈니스 코드를 UserAccountService로 분리
try-catch와 BindingResult 객체 대신 @RestControllerAdvice를 사용하여 예외 처리 로직 작성
예외 처리 로직을 ExceptionHandler, UnAuthorizedException, UserNotFoundException 파일로 분리

260608
UserAccountService의 리포지토리 중복상속구조를 UserAccountService -> UserAccountRepository -> UserAccountRepositoryCustom 상속구조로 변경
비밀번호 찾기와 관련된 로직 작성
1. UserAccountEntity에 Setter 대체 메서드 changePassword() 작성
2. UserFindPasswordRequestDTO, UserChangePasswordRequestDTO 작성 (클래스 대신 레코드 클래스 사용)
3. UserAccountAPIController에 verifyPassword(), updatePassword() 작성
4. UserAccountService에 verifyPassword(), updatePassword() 작성
5. UserAccountRepositoryCustomImpl에 findByUserAccountId(), findByUserAccountIdAndEmail() QueryDSL 메서드 작성
6. update_password.html, verify_password.html, update_password.js, verify_password.js 작성
7. SamePasswordHandler 파일 작성

260609
백엔드로 signup/login 시 입력하는 데이터를 보내기 전에 프론트엔드에서 자체 사전 검사(id/pw 조건)를 수행하는 로직을 각 JavaScript 파일에 작성
UnauthorizedException의 오류 메시지 수정
signup.js의 if (response.data.status == 'SUCCESS') 의 내부 조건 수정

260610
UserAccountService.createAccount()에 백엔드로 들어온 id 데이터에 대한 중복 검사 로직 작성
IllegalStateException과 IllegalArgumentException의 차이점에 대한 고찰
전자: 값은 문제 없지만 시스템의 상태에 맞지 않을 때 (ex-회원가입 시 중복 ID 기입)
후자: 사용자가 입력한 값 자체가 문제가 될 때 (ex-비밀번호 변경 시 이전 비밀번호 그대로 사용 시도)
적절한 해결법: 커스텀 Exception을 만들어서 사용
프론트엔드의 아이디 저장하기 기능에 관련된 코드 작성
1. html 파일의 적절한 위치에 th:value="${savedId}", th:checked="${rememberIdChecked}" 속성 추가
2. js 파일에서 data에 rememberId 필드 추가
3. UserAccountAPIController의 checkLogin()에 쿠키 사용을 위한 Cookie 라이브러리와 HttpServletResponse 라이브러리 추가하고 쿠키 설정 코드 작성
4. PageRenderController의 login()에 @CookieValue를 사용한 savedId 파라미터 추가하고 그에 관한 로직 작성

260611
USER_ACCOUNT 테이블에 nickname 컬럼 추가하고 관련된 부분들의 코드 수정
1. signup.html에 id="nickname"인 input 추가
2. signup.js에 프론트엔드 데이터검사 로직, data 데이터에 nickname 추가
3. UserAccountEntity에 nickname 필드 추가
4. UserRegisterRequestDTO에 nickname 필드 추가하고 @Valid 옵션 작성, toEntity Builder에 nickname 필드 추가
5. UserAccountRepository에 existsByNickname(String nickname) 메서드 선언
6. UserAccountService.createAccount에 중복 닉네임 검사 로직 추가
7. UserAccountAPIController.createAccount의 signupData에 entity.getNickname() 데이터 추가
BOARD 테이블 정의 변경
BoardEntity 엔티티 작성
UserAccountEntity와 BoardEntity의 @AllArgsConStructor 삭제 후 필수 인자로만 구성된 @Builder 커스텀 생성자 정의
CSRF 토큰&상단 메뉴용 조각 페이지 header.html 작성
CSRF 토큰 관련 Axios 설정용 공통JS파일 csrf-config.js 작성

260616-260617
UserAccountRepsitory의 existsById(String id) JPQL을 UserAccountRepositoryCustom의 existsByUserAccountId(String id) Query DSL로 변경
Spring Security용 UserDetailsCustom 작성
로그인 상세 처리 목적으로 UStat Enum 파일에 조건 2가지(Locked, Banned) 추가
UserAccountAPIController.checkLogin()에서 SecurityConfig/filterChain()으로 로그인 로직 이관
1. UserAccountAPIController.checkLogin()과 UserAccountService.checkLogin() 주석 처리
2. ROLE 설정 메서드와 ID/비밀번호 가져오기, 계정 권한 체크 등의 기능이 담긴 UserDetailsCustom 작성
3. 프로젝트의 기존 SELECT 작업(findByUserAccountId)을 UserDetails 인터페이스를 통해서 하도록 loadUserByUsername을 Override한 UserDetailsService 작성
4. SecurityConfig의 filterChain()에서 formLogin 관련 설정을 주석처리하고 커스텀 필터를 통해 로그인 작업을 수행하도록 코드 변경
5. 커스텀 필터 구현 클래스 JsonAuthenticationFilterCustom 작성
6. 커스텀 필터를 거쳐서 로그인에 성공했을 경우 실행되는 성공 핸들러 PracLoginSuccessHandler 작성
7. 커스텀 필터를 거쳐서 로그인에 실패했을 경우 실행되는 실패 핸들러 PracLoginFailureHandler 작성
8. 400/401 상태 분리를 임시 담당하던 PracExceptionHandler.handleUnauthorizedException() 주석처리, UnauthorizedException.java 삭제

260617
PageRenderController.board()에서 발생하는 N+1 문제를 해결하기 위해 BoardRepository.findAll() 내장함수 대신 BoardRepositoryImpl.findAllWithUser() 커스텀 Query DSL 함수로 변경하고 fetchJoin()이 포함된 Query 작성
@Column(name="") 옵션으로 BoardEntity의 일부 필드명이 BOARD 테이블의 컬럼명과 일치하지 않던 문제 해결
백엔드 -> 프론트엔드 응답 데이터 전송용 BoardShowListResponseDTO 레코드(불변 데이터 클래스) 작성
게시판 페이지 board.html 초안 작성
등록일 데이터 표기 변경을 위해 BoardShowListResponseDTO에 formattedRegdate() 작성

260618
게시판 상세 페이지 board-detail.html 초안 작성
board-detail.js에 화면 로딩 완료 후 조회수 증가 API 호출하는 코드 작성
BoardRepository에 동시성 이슈(Race Condition)를 방지하기 위해 @Modifying을 붙인 JPQL Atomic Query increaseViews() 작성
BoardService에 조회수 증가 메서드 increaseViews() 작성
BoardAPIController에 조회수 증가 메서드 increaseViews() 작성

260622
board-detail.html 초안 작성
board.css 작성
오류 & 버그 수정
글쓰기 기능에 로그인 상태 조건 추가

260623
게시글 등록 로직 구현
1. board-write.html 작성
2. 글쓰기 버튼 클릭 시 Axios를 사용해 prac/api/board/post 주소로 게시글 등록 API 호출하도록 board.js 작성
3. 템플릿 페이지 - > 백엔드로 게시글 데이터를 가지고 이동할 BoardCreateRequestDTO 작성
4. 게시글 데이터와 @AuthenticationPrincipal 객체로 로그인한 사용자 정보 중 @id를 전달하는 BoardAPIController.createPost() 작성
5. @id로 DB 조회 후 가져온 엔티티 데이터로 게시글 작성자 계정상태 검증 & 게시글 데이터 저장하는 BoardService.createPost() 작성

260625
board-delete.html과 board-delete.js 작성했으나 로직 변경으로 폐기
게시글 삭제 로직 구현
1. board-detail.js에 deleteButton관련 코드 작성 작성
2. BoardAPIController.deletePost() 작성
3. BoardService.deletePost() 작성
게시글 수정 로직 구현
1. board-update.html 작성
2. board-update.js 작성
3. PageRengerController.boardDisplayDetail() 작성 (board-detail.html에서 수정 버튼을 누르면 board-update.html에 수정 데이터 적재 후 로드)
4. BoardAPIController.updatePost() 작성 (board-update.html에서 수정 버튼을 누르면 게시글 UPDATE 절차 진행)
5. BoardService.updatePost() 작성 (실제 절차가 진행되는 곳)

260701
댓글 작성/삭제 백엔드 로직 구현
1. Oracle DB에 REPLY 테이블 생성
2. ReplyEntity 작성
3. BoardAPIController에 createReply, deleteReply 작성
4. BoardService에 createReply, deleteReply 작성
5. ReplyRepository, ReplyRepostoryCustom, ReplyRepositoryImpl 작성
6. ReplyCreateRequestDTO 작성

260702
권한 검증이 필요한 PageRenderController와 BoardAPIController의 일부 method에 @PreAuthorize 추가
댓글 데이터의 페이징 출력 로직 구현
1. ReplyRepository.findByBoardSeqWithUser를 List형에서 Page형으로 변경 & Pageable 파라미터 추가 & Query DSL method를 JPA Paging 방식으로 수정
2. ReplyShowListResponseDTO 레코드 작성
3. BoardService.getUserAccountIdForReply() 작성
4. PageRenderController.boardReadDetailPage()에 page 파라미터 추가 & JPA Paging 방식의 댓글 데이터 출력 코드 추가
5. board-detail.html에 댓글 출력을 위한 <div class="detail-reply-display"> 태그와 그 하위 태그들 추가
6. board.js에 댓글 등록과 관련된 if (replyButton) 로직 추가
7. 댓글 출력/등록/삭제와 관련된 board.css 코드 추가
board.js에 댓글 삭제와 관련된 if (replyDeleteButton) 로직 추가
header.html에 로그인 시 머물렀던 페이지로 돌아가기 위해 URL Query Parameter를 기존 URL 뒤에 인코딩해 붙이는 JS 코드 작성
login.js에 URL Query Parameter를 획득하고 디코딩하여 redirect 하는 로직 추가

260706
페이징 출력 로직의 잠재적 오류 발생가능 지점 보완
1. Page.getTotalPages()의 값을 담는 int 변수와 redirect 옵션을 사용하여 게시글과 댓글 페이지보다 큰 수가 요청되었을 경우 각각의 방어 코드 작성
board-detail.html의 댓글 페이징 출력 html 소스와 관련 css 소스 작성

260706~260708
board.html의 게시글 제목 출력 시 댓글 카운팅 정보를 같이 출력하는 로직에서 발생하는 N+1 문제와 동시성 이슈를 방지하기 위해 Redis 도입
1. Docker Desktop 설치 후 Redis 이미지를 내장한 rdwgall-redis Container 생성
2. build.gradle에 Redis 관련 Dependency 작성
3. application.yml에 Redis 관련 호스트명, 포트번호 지정
4. config/RedisConfig.java 설정 파일 작성
5. BoardService.increaseViews()에 Redis 구조 도입하여 수정

260709
지정 시간마다 작업을 수행하는 RedisViewCountScheduler.java 스케줄러 작성

260716
application.yml에 스케줄링 스레드 풀 크기 확장
Service와 Redis Scheduler에서 각자 다른 Key 방식을 사용함으로써 생기는 매핑 불일치 버그 해결
PageRenderController에 존재하는 비즈니스 로직을 BoardService.readBoardList(), BoardService.readPost(), BoardService.getPostedData(), BoardService.readReplyList()로 이관하여 계층간 결합도 상승 문제 해결
BoardShowPostResponseDTO 작성하여 게시글 상세 페이지 조회 시 Entity 직접 전달 대신 DTO 전달 체계로 수정
board-detail.js 내 if (boardSeq) 문의 axios API에 then() 행동문을 추가하여 조회수 + 1이 프론트엔드 내에서 바로 반영되게 하는 로직 작성

260718
board-detail.js에 replyDeleteButton과 관련된 클릭 감지/태그 교체 로직의 최적화와 오류 수정 작업 진행
추천/비추천 관련 기능 구현
1. RDB, BoardEntity, BoardShowPostsResponseDTO에 dislikes 필드 추가
2. board-detail.html에 추천/비추천 관련 상자 디자인 추가
3. board-detail.js에 추천/비추천 버튼 클릭 시 추천수/비추천수 증가 로직 작성
4. RedisLikeCountScheduler.java 스케줄러 작성
5. BoardService.increaseLikes(), BoardService.increaseDislikes() 작성 (opsForValue().setIfAbsent()를 활용한 Redis TTL 설정으로 매일 자정에 추천/비추천 재활성화)

260719-260720
BoardService.readBoardList()와 BoardService.readPost()의 views 값 계산 시 논리적 오류 수정
마이페이지 관련 백엔드 기능 구현
1. ManageShowUserDataResponseDTO 초안 작성
2. BoardRepository와 ReplyRepository에 사용자PK를 받아서 최근 작성 데이터 n개를 출력하는 List형 Query Method findByUserEntity_SeqOrderByRegdateDesc() 작성
3. ManageService 생성 후 getManageDatas() 초안 작성
4. PageRenderController.mypage() 초안 작성

260720
커스텀 Exception 클래스 파일들의 반환 오류코드와 파일명 구체화
마이페이지 닉네임 변경 관련 기능 구현
1. ManageChangeNicknameRequestDTO 작성
2. NicknameDuplicatedException, NicknameNotChangedException 작성
3. PracAPIExceptionHandler(PracExceptionHandler의 이름 변경 버전)와 PracPageExceptionHandler로 @Controller와 @RestController의 오류 담당 핸들러 분리
4. PageRenderController.myPage()의 파라미터와 GetMapping 주소 변경
5. ManageAPIController.changeNickname() 작성
6. ManageService.getManageDatas()의 파라미터 변경
7. ManageService.changeNickname() 초안 작성

260723
닉네임 변경
ManageAPIController.changeNickname()을 changeMyNickname()과 changeNicknameByAdmin()으로 분리
ManageAPIControllerchangeUserStatus() 작성
ManageService.updateNickname(), changeMyNickname(), changeNicknameByAdmin(), changeUserStatusByAdmin() 작성
Session Redis 사용을 위한 RedisSessionConfig 작성
사용자 세션 제어를 위해 UserDetailsCustom과 UserEntity에 직렬화 버전ID 변수 명시
Nickname UPDATE 관련하여 UserAccountEntity에 @DynamicUpdate 추가

260727
중복로그인 제어와 직렬화/역직렬화 관련 예외 처리
1. SecurityConfig에 Spring Session Redis의 IndexedSessionRepository가 포함된 Session Registry Bean 추가
2. SecurityConfig.filterChain에 http.sessionManagement로 동시 세션 제어 설정 추가
3. SecurityConfig.filterChain의 http.logout 설정에서 세션 쿠키의 이름을 Session Redis 기본 이름에 맞게 수정
4. Session 관련 정보를 담는 SessionDTO.java 작성
5. 로그인 정보가 담긴 UserDetailsCustom이 UserAccountEntity를 직접 참조하는 대신 SessionDTO를 참조하도록 코드 수정
6. PageRenderController, BoardAPIController, ManageAPIController에서 UserDetailsCustom의 요소를 참조하는 코드가 UserAccountEntity대신 SessionDTO를 거치도록 수정
7. RedisSessionConfig.objectMapper()에 역직렬화를 허용할 클래스 파일을 mapper.addMixIn()으로 추가하고 SecurityAllowlistMixin 추상 클래스 선언
8. UserDetailsCustom에 @JsonIgnoreProperties(ignoreUnknown = true)를 추가
9. UserDetailsCustom에 @JsonCreator와 @JsonProperty로 private final 필드가 포함된 생성자를 명시
10. UserDetailsCustom 일부 메서드에 @JsonIgnore로 직렬화 제외 처리

260728
관리페이지의 댓글 데이터 출력 관련 로직 수정 (댓글이 달린 게시글의 제목 데이터 추가 출력)
1. ManageShowUserDataResponseDTO.ReplyData에 게시글 정보 출력을 위해 boardSeq, title 필드 추가
2. 기존의 ReplyRepository.findByUserEntity_SeqOrderByRegdateDesc()를 삭제
3. ReplyRepositoryCustom에 findReplyDatasWithBoardTitleByUserSeq()의 선언부를, ReplyRepositoryImpl에 구현부를 작성
4. ManageService.getManageDatas()에서 replyDatas 데이터 초기화 부분 수정

260729
mypage.html 작성

260730-260731
mypage.js 작성
추천수와 비추천수를 프론트엔드에 실시간으로 반영시키기 위해 BoardShowPostResponseDTO의 추천수/비추천수 관련 정적 팩토리 메서드에 likesDelta, dislikesDelta 값을 파라미터로 받고 관련 변수에 추가
BoardService.readPost()에서 조회수만 실시간 반영하던 단일 Hash Get() 요청을 Hash Multi Get() 요청으로 수정하고, 조회수/추천수/비추천수 delta 값을 세팅하기 위해 타입캐스팅과 파싱 작업을 수행하는 부분을 parseDelta()로 분리
readBoardList(), readPost()에서 변경된 Hash Key 방식을 따라서 increaseViews(), increaseDislikes()의 key 선언 문자열과 opsForHash().increments() 인자 수정
RedisViewCountScheduler와 RedisLikeCountScheduler에서 n주기 이상의 서버 장애를 이유로 동기화 처리에 실패한 Key 데이터가 그대로 unlink()로 유실되는 논리 오류 수정
Scheduler와 Service 간 Hash Key 구조가 맞지 않는 문제 수정

260806
<String, Object>형의 RedisTemplate 설정파일의 value 대상 직렬화 도구를 GenericJackson2JsonRedisSerializer로 변경하고 LocalDateTime/ZonedDateTime 지원하는 ObjectMapper 옵션 추가
기존 RedisTemplate 라이브러리 관련 의존 주입 코드(BoardService, RedisViewCountScheduler, RedisLikeCountScheduler)는 StringRedisTemplate 라이브러리를 사용하도록 변경
verify-user.html(verify-password.html 에서 이름 변경), update-password.html의 CSS 적용 오류 수정
UserAccountService에서 verifyUser() 다음 단계로 3분의 TTL을 가지는 임시 토큰 발행 로직 초안 작성
악의적 조회수 증가 요청에 대응하도록 Redis Key 토큰 생성 후 검증하는 로직 작성
1. BoardService.increaseViewsVerifyToken()으로 Redis Key 토큰 생성하는 method 작성
2. BoardService.readPost()에서 findById()로 실제 게시글 여부 검증 후 토큰 생성 method 호출하여 토큰 발급
3. BoardShowPostResponseDTO에 토큰 관련 필드 추가해서 토큰이 프론트엔드에 전달될 수 있도록 조치
4. board-detail.html에서 th:data-views-token="${board.viewsToken}" 속성으로 토큰 데이터에 접근
5. board-detail.js에서 querySelector로 토큰 데이터를 가져오고, 페이지 렌더가 끝난 후 BoardService.increaseViews()를 호출할 때 토큰 데이터를 백엔드로 같이 전송
6. BoardAPIController.increaseViews()의 파라미터에 @RequestBody BoardIncreaseViewsRequestDTO viewsDTO를 추가하고 increaseViews()의 파라미터에도 viewsDTO 추가
7. BoardService.increaseViews()는 전달받은 viewsDTO 데이터를 가지고 Redis에 해당 데이터를 name으로 하는 Redis Key 가 존재하는지 검증

260807
비밀번호 찾기 기능에서 HTTPSession + SessionStorage 대신 Redis Key + SessionStorage를 사용하여 데이터를 검증하도록 5개 파일의 코드 변경
1. UserAccountService: verifyUser(), resetPasswordToken(), updatePassword()
2. UserAccountAPIController: verifyUser()
3. verify-user.js
4. update-password.js
5. UserUpdatePasswordRequestDTO.java

260810
관리자 페이지 구현
1. PageRenderController.admin()에 페이징 기술 적용
2. ManageService.getUserList() 작성
3. adminpage.html 작성
4. adminpage.css 초안 작성
5. adminpage.js 작성
